# Video Capture and Encoding with FFmpeg
# ========================================
# Este archivo contiene las dependencias de Python requeridas
# para el sistema de captura y codificación de video

# ffmpeg-python no es estrictamente necesario ya que usamos subprocess
# directamente, pero puede ser útil para proyectos más complejos
# ffmpeg-python==0.2.3

# Logging y utilidades (opcionales)
# colorlog==6.7.0  # Para logs coloreados

# Nota: FFmpeg debe estar instalado en el sistema (no en pip)
# Ver INSTALL.md para instrucciones de instalación específicas del SO
